export enum WeekDay {
    first = 1,
    last = 7
}

export enum RoomsRange {
    min = 1,
    max = 8
}

export enum GuestsRange {
    min = 1,
    max = 10
}

export enum RentalPriceRange {
    min = 100,
    max = 100000
}

export enum CommentsCount {
    min = 0,
    max = 20
}
